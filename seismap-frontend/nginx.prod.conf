##
## nginx config – PRODUCTION mode
## Swagger UI, api-docs, GeoServer Web UI, the GeoServer REST admin API and
## the backend's data-loading admin endpoints are blocked (404).
## GeoServer WMS/WFS tiles are still proxied (needed by the map).
##
## Two server blocks share the same locations (see locations.conf):
##  - :80  plain HTTP, for LAN/local access (e.g. host port 3000)
##  - :443 TLS, for the internet-facing DuckDNS domain
##

server {
    listen 80;
    server_name _;
    absolute_redirect off;

    root /usr/share/nginx/html;
    index index.html;

    # Log all requests to file in production
    access_log /var/log/nginx/access.log combined;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml
               application/xml application/xml+rss text/javascript;

    include /etc/nginx/locations.conf;
}

server {
    listen 443 ssl;
    server_name _;
    absolute_redirect off;

    # Populated at container start by docker-entrypoint.sh — either a symlink
    # to the real Let's Encrypt cert, or a self-signed fallback for local dev.
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    root /usr/share/nginx/html;
    index index.html;

    access_log /var/log/nginx/access.log combined;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml
               application/xml application/xml+rss text/javascript;

    include /etc/nginx/locations.conf;
}
