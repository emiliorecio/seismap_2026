spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/seismap_test
    username: seismap
    password: seismap
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
